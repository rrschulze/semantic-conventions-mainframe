Add metric refinements that extend upstream OpenTelemetry semantic conventions with IBM Z-specific attributes and dimensions:

- **Network (port and NIC)**: refinements of `system.network.io`, `system.network.packet.count`, `system.network.packet.dropped`, `system.network.errors`, and `system.network.connection.count` scoped to mainframe physical adapter ports and partition virtual NICs
- **Storage**: refinement of `system.disk.io` extended for IBM Z FICON and zHyperLink channel paths
- **System CPU**: refinements of `system.cpu.utilization` and `system.cpu.time` extended with IBM Z processor type dimensions (general purpose, IFL, zIIP, ICF, SAP); refinement of `system.paging.operations` extended with z/VM hypervisor storage tiers
