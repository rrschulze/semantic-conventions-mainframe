Suppress the Weaver `UnstableFileFormat` warning that was emitted for all v2 schema files; added a `gh_workflow_command` diagnostic template so validation errors are surfaced as GitHub Actions annotations.
