Add `mainframe` entity group with three new entities — `mainframe.channel`, `mainframe.adapter`, and `mainframe.cpu` — and entity refinements that map the IBM Z Central Processing Complex (CPC) to `virtualization.platform`, an LPAR to `virtualization.partition`, a shared processor pool to `virtualization.resource_pool`, z/VM CP to `virtualization.hypervisor`, a z/VM guest to `virtualization.vm`, and a DPM storage group to `virtualization.storage_pool`.
