{% import 'attribute_macros.j2' as attrs %}
{% macro snippet(attributes) %}{% set sampling_attributes = attributes | selectattr("sampling_relevant", "true") | list %}{% if sampling_attributes %}
The following attributes can be important for making sampling decisions
and SHOULD be provided **at span creation time** (if provided at all):

{% for attribute in sampling_attributes | sort(attribute="key") %}* {{ attrs.name_with_link(attribute) }}
{% endfor %}{% endif %}{% endmacro %}
