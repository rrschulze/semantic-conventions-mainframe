# Weaver project configuration.
# Applies to every template package this project uses (docs generation, snippets).
# See: https://github.com/open-telemetry/opentelemetry-weaver-packages/blob/main/templates/docs/markdown/README.md

[template]
# Acronyms are upper-cased in namespace headings and signal titles.
acronyms = ["CPU", "CPC", "LPAR", "HMC", "DPM", "VFM", "SMF", "I/O", "NIC", "HBA"]

# Human-readable namespace titles used when the id is not a simple capitalization.
# Any namespace id not listed here is title-cased (e.g. "mainframe" -> "Mainframe").
[template.text_maps.namespace_mapping]
zos = "z/OS"

# Where upstream (OTel base registry) attribute documentation lives, keyed by
# the schema_url declared in model/manifest.yaml's dependencies block.
# Attributes from that dependency link to these pages.
# NOTE: This key must match the schema_url in model/manifest.yaml's dependencies
# block, which is auto-patched by the filter-upstream Make target to match
# SEMCONV_VERSION. When Renovate bumps SEMCONV_VERSION in versions.env this
# entry must be updated manually to the new version (both the key and the URL).
# The key format is always: "https://opentelemetry.io/schemas/<version>"
# The URL format is:        "https://github.com/open-telemetry/semantic-conventions/blob/v<version>/docs/registry/attributes/{namespace}.md"
[template.text_maps.upstream_docs]
"https://opentelemetry.io/schemas/1.42.0" = "https://github.com/open-telemetry/semantic-conventions/blob/v1.42.0/docs/registry/attributes/{namespace}.md"
